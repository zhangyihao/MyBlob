<!DOCTYPE html>
<html lang="zh-cn">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>编辑文章</title>
    <!-- Bootstrap -->
    <link href='/stylesheets/style.css' rel='stylesheet' />
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/stylesheets/main.css" rel="stylesheet">
</head>
<body>

<%- include('nav') %>

<div class="container main">
    <div class="row">
        <% if(success){ %>
        <div id="message" class="alert alert-success col-md-12 col-xs-12"><%= success %></div>
        <% }%>
        <% if(error){ %>
        <div id="message" class="alert alert-danger col-md-12 col-xs-12"><%= error %></div>
        <% }%>
    </div>
    <form class="form-signin" method="post" enctype="multipart/form-data" action="/posts/<%= post._id %>/edit">
        <div class="row panel panel-default">
            <div class="col-xs-12 col-md-12 panel-body">
                <div class="form-group">
                    <label>文章标题</label>
                    <input class="form-control" name="title" type="text" value="<%= post.title %>" placeholder="请输入文章标题" />
                </div>
                <div class="form-group">
                    <label>内容</label>
                    <textarea class="form-control" id="content" name="content" rows="20" ><%= post.content %></textarea>
                </div>
                <button type="submit" class="btn btn-primary btn-block">发表</button>
            </div>
        </div>
    </form>
</div>

<!-- 如果要使用Bootstrap的js插件，必须先调入jQuery -->
<script src="/javascript/jquery-3.1.1.min.js"></script>
<!-- 包括所有bootstrap的js插件或者可以根据需要使用的js插件调用　-->
<script src="/bootstrap/bootstrap.min.js"></script>
<script>
  setTimeout(function () {
    $('#message').fadeOut();
  }, 3000);
</script>
</body>
</html>