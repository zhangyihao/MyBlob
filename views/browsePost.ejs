<!DOCTYPE html>
<html lang="zh-cn">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>文章</title>
    <!-- Bootstrap -->
    <link href='/stylesheets/style.css' rel='stylesheet' />
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/stylesheets/main.css" rel="stylesheet">
    <style type="text/css">
        .content-foot {
            float: right;
        }
        .content-foot li {
            float: right;
            list-style: none;
            margin-right: 5px;
        }
        .form-comment {
            margin: auto 10px;
        }
    </style>
</head>
<body>

<%- include('nav') %>

<div class="container">
    <div class="panel panel-default">
        <div class="panel-body">
            <h2><%= post.title %></h2>
            <p>发布时间：<%= post.created_at %>&nbsp;&nbsp;&nbsp;&nbsp;浏览量：<%= post.pv %></p>
            <p><%- post.content %></p>
            <div >
                <ul class="content-foot">
                    <% if(typeof(user) !=='undefined' && user!==null && typeof(post.author._id)!=='undefined'
                            && post.author._id.toString() === user._id.toString()) { %>
                    <li><a href="/posts/<%=post._id%>/remove">删除</a></li>
                    <li><a href="/posts/<%=post._id%>/edit">编辑</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </div>
    <%- include('comments') %>
</div>

<!-- 如果要使用Bootstrap的js插件，必须先调入jQuery -->
<script src="/javascript/jquery-3.1.1.min.js"></script>
<!-- 包括所有bootstrap的js插件或者可以根据需要使用的js插件调用　-->
<script src="/bootstrap/js/bootstrap.min.js"></script>
</body>
</html>